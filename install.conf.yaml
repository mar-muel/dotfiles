- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.vimrc: vimrc
    ~/.vim: vim
    ~/.bashrc: bashrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
